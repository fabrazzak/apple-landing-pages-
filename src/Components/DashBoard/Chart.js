import React from 'react';

const Chart = (props) => {
    
    const {month,investment,sell,revenue}=props.data;
    return (
        <div>
            <h1></h1>
        </div>
    );
};

export default Chart;